// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // 网站最终网址（GitHub Pages 用户站，根路径）。换主机就改这里。
  site: 'https://justin-zyc.github.io',
});
