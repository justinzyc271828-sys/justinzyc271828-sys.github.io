---
import { getCollection } from 'astro:content';
import BaseLayout from '../../layouts/BaseLayout.astro';
import SectionArchive from '../../components/SectionArchive.astro';
import Subscribe from '../../components/Subscribe.astro';
const posts = await getCollection('de');
---

<BaseLayout title="德语语法笔记 · Grenzgang" description="在柏林学德语路上的语法笔记 —— 把一道道格与时态的关，一个个拆开。" active="de">
  <SectionArchive sectionKey="de" title="德语语法笔记" subtitle="语言是一道道格与时态的关 —— 一个个拆开。" posts={posts} />
  <Subscribe />
</BaseLayout>
